<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:gravity="center_horizontal"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginLeft="20dp"
    android:layout_marginRight="20dp"
    >

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/lib_tab_design_line"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="-20.0dip"
        android:src="@drawable/ic_type_selected_tag"
        android:layout_below="@id/lib_tab_design_title"
        android:layout_alignStart="@id/lib_tab_design_title"
        android:visibility="gone"
        android:layout_alignEnd="@id/lib_tab_design_title" />

    <TextView
        android:id="@+id/lib_tab_design_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textSize="17sp"
        android:textColor="@color/black"
        />

</RelativeLayout>